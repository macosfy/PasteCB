Place paired screenshots here for local token comparison, for example:

- `img-eval-original.png`
- `img-eval-compressed.png`

Paths can be overridden with `IMAGE_ORIGINAL_PATH` and `IMAGE_COMPRESSED_PATH` in `.env`.
